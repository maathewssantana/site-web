pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Exemplo de comando para construir o projeto
                sh 'npm install'
                sh 'npm run build'
            }
        }
    }
}
